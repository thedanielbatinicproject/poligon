import React from "react";

const AdminPanel: React.FC = () => (
  <div>
    <h1>Admin Panel</h1>
    <p>Centralized administration for users, documents, types, and tasks.</p>
  </div>
);

export default AdminPanel;
