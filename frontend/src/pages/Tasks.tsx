import React from 'react';

const Tasks: React.FC = () => (
  <section>
    <h1 className="text-2xl font-bold">Tasks</h1>
    <p className="mt-2 text-gray-700">Tasks placeholder.</p>
  </section>
);

export default Tasks;
